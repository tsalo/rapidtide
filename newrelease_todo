+ Edit CHANGELOG.md (use git log to get the history)

+ Sync everything to github.

+ Create a new release on GitHub (https://github.com/bbfrederick/rapidtide/releases)

+ GitHub Actions will publish the new release to PyPi and a Docker image to Docker Hub
